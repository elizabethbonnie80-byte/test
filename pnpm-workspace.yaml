allowBuilds:
  sharp: true
  supabase: true
  # eslint-config-next's import resolver ships a prebuilt native binding, so its
  # postinstall build step is unnecessary — ESLint runs fine without it.
  unrs-resolver: false
onlyBuiltDependencies:
  - sharp
  - supabase
